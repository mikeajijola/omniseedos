import fs from 'node:fs';import path from 'node:path';import {resolveTransport} from '../lib/omniseed/config.mjs';import {loadOperatingView,renderOperatingView} from '../lib/ui/render.mjs';import {renderDemoFounding} from '../lib/ui/founding.mjs';
fs.rmSync('dist',{recursive:true,force:true});fs.mkdirSync('dist',{recursive:true});const fixture=JSON.parse(fs.readFileSync('fixtures/startup.json'));const mode=process.env.OMNISEED_TRANSPORT_MODE||'demo';if(mode==='live')throw new Error('Static builds cannot embed LiveTransport; deploy demo or fixture mode');const transport=resolveTransport({mode,fixture});const view=await loadOperatingView(transport),operating=renderOperatingView(view,{transportName:mode});write('index.html',operating);for(const route of ['company','capabilities','plan','observe','activity'])write(`${route}/index.html`,operating);write('found/index.html',renderDemoFounding());write('eve/index.html',renderEve(view));console.log(`Built accessible ${mode} OmniSeed OS with 8 review routes into dist/.`);
function write(file,content){const target=path.join('dist',file);fs.mkdirSync(path.dirname(target),{recursive:true});fs.writeFileSync(target,content)}
function renderEve(view){return `<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width"><title>Eve — OmniSeed OS</title><style>body{max-width:760px;margin:auto;padding:24px;font:16px/1.6 system-ui;background:#f7f9f7;color:#17201d}main{background:white;padding:28px;border:1px solid #dce3df;border-radius:16px}a{color:#176b4d}.mode{background:#fff4ce;padding:5px 9px;border-radius:99px}</style><main><a href="/">← OmniSeed OS</a><p><span class="mode">Demo · read-only</span></p><h1>Eve</h1><p>Conversational access to the same structured runtime operations.</p><h2>Example live answers</h2><p><strong>What capabilities are missing?</strong></p><ul>${view.gaps.map(gap=>`<li>${gap.name}</li>`).join('')}</ul><p>In this public demo Eve is illustrative and cannot mutate or persist state. Local live mode wires Eve to OmniSeed through LiveTransport.</p></main></html>`}
