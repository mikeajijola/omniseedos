export default async function handler(request,response){if(request.method!=='POST'){response.statusCode=405;return response.end('Method not allowed')}const chunks=[];for await(const chunk of request)chunks.push(chunk);const key=new URLSearchParams(Buffer.concat(chunks).toString()).get('key')||'',expected=process.env.OMNISEED_OWNER_TOKEN||'';if(!expected||!equal(key,expected)){response.statusCode=403;return response.end('Invalid owner access key')}response.statusCode=303;response.setHeader('location','/');response.setHeader('set-cookie',`omniseed_owner=${encodeURIComponent(key)}; Path=/; HttpOnly; Secure; SameSite=Lax; Max-Age=28800`);response.end()}
function equal(value,expected){if(value.length!==expected.length)return false;let result=0;for(let index=0;index<value.length;index++)result|=value.charCodeAt(index)^expected.charCodeAt(index);return result===0}
