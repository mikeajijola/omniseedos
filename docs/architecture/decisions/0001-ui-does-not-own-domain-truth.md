# ADR 0001: UI does not own domain truth

- Status: Accepted

OmniSeed OS consumes calculated state, plans, findings, evidence, drift, and events from OmniSeed. It does not reproduce their rules in React components. This keeps every actor interface consistent.
