# System boundaries

OmniSeed OS is an interface to shared capabilities. Omniform owns meaning; OmniSeed owns validation, state, plans, authorization flow, apply, evidence, drift, findings, and events. UI components render those outputs. Eve reads them through tools and later proposes mutations through the same plan/policy/apply path. Physical actors use structured commands or task assignments without changing business capability semantics.
