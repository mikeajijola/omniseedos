import test from 'node:test';import assert from 'node:assert/strict';import fs from 'node:fs';
test('static deployment contains every review route and truthful operating shell',()=>{for(const route of ['index.html','found/index.html','company/index.html','capabilities/index.html','plan/index.html','observe/index.html','activity/index.html','eve/index.html'])assert.ok(fs.statSync(`dist/${route}`).isFile(),route);const home=fs.readFileSync('dist/index.html','utf8'),found=fs.readFileSync('dist/found/index.html','utf8');assert.match(home,/Demo · read-only/);assert.match(home,/Customer Support/);assert.match(home,/What needs my attention\?/);assert.match(home,/Needs attention/);assert.match(home,/aria-label="Primary"/);assert.match(home,/@media\(max-width:620px\)/);assert.match(found,/Nothing is persisted/i);assert.match(found,/Ask Eve/)});
